{{> navbar}}
  <div class="container mt-5">
    <h2>Repartidores (Delivery)</h2>
    <a href="/deliverys/create" class="btn btn-success mb-3">Agregar Delivery</a>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>#</th>
          <th>Nombre</th>
          <th>Teléfono</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        {{#each deliverys}}
        <tr>
          <td>{{id}}</td>
          <td>{{nombre}}</td>
          <td>{{telefono}}</td>
          <td>
            <a href="/deliverys/edit/{{id}}" class="btn btn-warning btn-sm">Editar</a>
            <form action="/deliverys/delete/{{id}}" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>


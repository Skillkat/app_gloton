<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Lista de Clientes</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <h2 class="mb-4">Clientes</h2>
    <a href="/clientes/create" class="btn btn-primary mb-3">Agregar Cliente</a>
    <table class="table table-bordered">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Nombre</th>
          <th>Correo</th>
          <th>Teléfono</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        {{#each clientes}}
        <tr>
          <td>{{id}}</td>
          <td>{{nombre}}</td>
          <td>{{correo}}</td>
          <td>{{telefono}}</td>
          <td>
            <a href="/clientes/edit/{{id}}" class="btn btn-warning btn-sm">Editar</a>
            <form action="/clientes/delete/{{id}}" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>
